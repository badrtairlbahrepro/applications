// Fonctions utilitaires pour générer le mot de passe function generatePassword(length, options) {const upper = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ';const lower = 'abcdefghijklmnopqrstuvwxyz';const numbers = '0123456789';const symbols = '!@#$%^&*()-_=+[]{}|;:,.<>?/';let chars = '';if (options.uppercase) chars += upper;if (options.lowercase) chars += lower;if (options.numbers) chars += numbers;if (options.symbols) chars += symbols;if (chars.length === 0) return '';let password = '';for (let i = 0; i < length; i++) {password += chars.charAt(Math.floor(Math.random() * chars.length));}return password;}document.addEventListener('DOMContentLoaded', () => {const form = document.getElementById('passwordForm');const lengthInput = document.getElementById('length');const lengthValue = document.getElementById('lengthValue');const output = document.getElementById('passwordOutput');const copyBtn = document.getElementById('copyBtn');lengthInput.addEventListener('input', () => {lengthValue.textContent = lengthInput.value;lengthInput.setAttribute('aria-valuenow', lengthInput.value);});form.addEventListener('submit', (e) => {e.preventDefault();const options = {uppercase: document.getElementById('uppercase').checked,lowercase: document.getElementById('lowercase').checked,numbers: document.getElementById('numbers').checked,symbols: document.getElementById('symbols').checked,};const length = parseInt(lengthInput.value, 10);const password = generatePassword(length, options);output.value = password || 'Veuillez sélectionner au moins une option.';});copyBtn.addEventListener('click', () => {const pwd = output.value;if (!pwd || pwd === 'Veuillez sélectionner au moins une option.') return;navigator.clipboard.writeText(pwd).then(() => {copyBtn.textContent = 'Copié !';setTimeout(() => {copyBtn.textContent = 'Copier';}, 2000);}).catch(() => {copyBtn.textContent = 'Erreur';});});});